{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "costgraph-selfhosted.fullname" . }}
  labels:
    {{- include "costgraph-selfhosted.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- if $.Values.dashboard.enabled }}
          {{/*
          The API is matched first: the dashboard's catch-all would otherwise
          swallow /api and the browser would get index.html for every call.
          */}}
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "costgraph-selfhosted.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          - path: /healthz
            pathType: Prefix
            backend:
              service:
                name: {{ include "costgraph-selfhosted.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if $.Values.dashboard.enabled }}
                name: {{ include "costgraph-selfhosted.fullname" $ }}-dashboard
                port:
                  number: 80
                {{- else }}
                name: {{ include "costgraph-selfhosted.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
